import 'package:flutter/material.dart';

class FeedbackFormPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Feedback')),
      body: Center(
        child: Text('Formulário de feedback aqui.'),
      ),
    );
  }
}